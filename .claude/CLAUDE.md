# Schedule project

@../DB/AGENTS.md

Read the shared rules before any database edit or maintenance command. Database-only edits must keep the independently installed natural-language request service running. Do not run stop.ps1 or install.ps1 for schedule changes. The local DB/CLAUDE.md retains personal notes, but DB/AGENTS.md defines the current paths and service lifecycle.
